# Swagger Documentation Update Report

## Обновление от: 19 декабря 2025

### Добавленные endpoints:

#### 🆕 POST /auth/max
- **Описание**: Аутентификация пользователей MAX Mini App
- **Назначение**: Валидирует initData от MAX платформы и возвращает JWT токены
- **Теги**: auth
- **Входные данные**: 
  ```json
  {
    "init_data": "string (required)"
  }
  ```
- **Ответы**:
  - **200**: Успешная аутентификация
    ```json
    {
      "access_token": "string",
      "refresh_token": "string"
    }
    ```
  - **400**: Неверный запрос
  - **401**: Ошибка аутентификации
  - **500**: Внутренняя ошибка сервера

### Обновленные модели данных:

#### 🔄 domain.User
Добавлены новые поля для поддержки MAX Mini App:
- `max_id`: Уникальный идентификатор пользователя в MAX платформе
- `username`: Имя пользователя в MAX
- `name`: Отображаемое имя из MAX

#### 🆕 http.MaxAuthRequest
Новая модель для запросов MAX аутентификации:
- `init_data`: Строка с данными инициализации от MAX (обязательное поле)

#### 🆕 http.MaxAuthResponse
Новая модель для ответов MAX аутентификации:
- `access_token`: JWT токен доступа
- `refresh_token`: JWT токен обновления

### Файлы обновлены:
- ✅ `internal/infrastructure/http/docs/swagger.json`
- ✅ `internal/infrastructure/http/docs/swagger.yaml`
- ✅ `internal/infrastructure/http/docs/docs.go`

### Команда для обновления:
```bash
~/go/bin/swag init -g cmd/auth/main.go -o internal/infrastructure/http/docs
```

### Проверка:
Swagger документация успешно сгенерирована и включает все новые endpoints и модели данных для MAX Mini App аутентификации.