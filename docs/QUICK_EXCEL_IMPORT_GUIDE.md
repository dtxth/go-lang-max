# 🚀 Быстрый старт: Импорт Excel

## 3 шага до импорта

### 1️⃣ Применить миграции (30 сек)

```bash
./apply_excel_migration.sh
```

### 2️⃣ Пересобрать сервисы (2 мин)

```bash
docker-compose up -d --build chat-service migration-service
```

### 3️⃣ Импортировать Excel

```bash
curl -X POST http://localhost:8084/migration/excel \
  -F "file=@Наименование чатов групп МГТУ в МАХ 17.11.25_ИТОГ.xlsx"
```

**Для больших файлов (20+ МБ):** См. [LARGE_EXCEL_IMPORT_GUIDE.md](LARGE_EXCEL_IMPORT_GUIDE.md)

## Проверка результата

```bash
# Статус импорта
curl http://localhost:8084/migration/jobs/1

# Созданные чаты
curl http://localhost:8082/chats

# Конкретный чат
curl http://localhost:8082/chats/1
```

## Что импортируется

Из каждой строки Excel создается:
- ✅ Университет (INN, КПП, название, ФОИВ)
- ✅ Филиал
- ✅ Факультет/колледж
- ✅ Группа (курс, номер)
- ✅ Чат (название, ссылка, external_chat_id)
- ✅ Администратор (телефон, max_id, флаги)

## Структура Excel (18 колонок)

```
0:  Телефон администратора
1:  max_id
2:  ИНН_Справочник (не используется)
3:  ФОИВ_Справочник
4:  Наименование организации
5:  Наименование филиала
6:  ИНН юридического лица
7:  КПП
8:  Факультет/институт
9:  Курс обучения
10: Номер группы
11: Название чата
12: Телефон администратора (резервный)
13: Наименование файла (логирование)
14: chat_id (external_chat_id)
15: link (URL чата)
16: add_user (ИСТИНА/FALSE)
17: add_admin (ИСТИНА/FALSE)
```

## Готово! 🎉

Подробности в [EXCEL_IMPORT_READY.md](EXCEL_IMPORT_READY.md)
