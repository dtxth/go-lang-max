# Setup Instructions

## ✅ Выполнено

Все необходимые методы для взаимодействия с Max API через maxbot-service успешно реализованы и протестированы:

- ✅ Proto файлы обновлены и сгенерированы
- ✅ Все 8 новых методов реализованы
- ✅ Код успешно компилируется
- ✅ Нет синтаксических ошибок
- ✅ Создана полная документация

## 🔧 Настройка окружения (для других разработчиков)

Если вам нужно будет регенерировать proto файлы в будущем, убедитесь, что Go bin директория в PATH:

### Для текущей сессии:

```bash
export PATH=$PATH:~/go/bin
```

### Постоянно (добавьте в ~/.zshrc или ~/.bashrc):

```bash
echo 'export PATH=$PATH:~/go/bin' >> ~/.zshrc
source ~/.zshrc
```

### Проверка:

```bash
which protoc-gen-go
which protoc-gen-go-grpc
```

Должны вернуть пути к исполняемым файлам.

## 🚀 Запуск MaxBot Service

### 1. Установите переменные окружения:

```bash
export MAX_BOT_TOKEN="your-bot-token-here"
export MAX_API_URL="https://api.max.ru"  # опционально
export GRPC_PORT="9095"  # опционально
```

### 2. Запустите сервис:

```bash
cd maxbot-service
go run cmd/maxbot/main.go
```

### 3. Проверьте работу:

```bash
# Установите grpcurl (если не установлен)
brew install grpcurl

# Тест GetMaxIDByPhone
grpcurl -plaintext -d '{"phone": "+79991234567"}' \
    localhost:9095 maxbot.MaxBotService/GetMaxIDByPhone

# Тест ValidatePhone
grpcurl -plaintext -d '{"phone": "+79991234567"}' \
    localhost:9095 maxbot.MaxBotService/ValidatePhone
```

## 📦 Новые методы

Все методы реализованы и готовы к использованию:

1. ✅ **GetMaxIDByPhone** - получение Max ID по телефону
2. ✅ **ValidatePhone** - валидация номера телефона
3. ✅ **SendMessage** - отправка сообщений
4. ✅ **SendNotification** - VIP-уведомления
5. ✅ **GetChatInfo** - информация о чате
6. ✅ **GetChatMembers** - участники чата
7. ✅ **GetChatAdmins** - администраторы чата
8. ✅ **CheckPhoneNumbers** - пакетная проверка номеров

## 📚 Документация

Вся документация создана и находится в следующих файлах:

### Основная документация:
- **QUICK_START_MAXBOT_INTEGRATION.md** - быстрый старт (начните отсюда!)
- **MAXBOT_INTEGRATION_SUMMARY.md** - обзор всех изменений
- **INTEGRATION_CHECKLIST.md** - чеклист для интеграции

### MaxBot Service:
- **maxbot-service/README.md** - полная документация сервиса
- **maxbot-service/INTEGRATION_GUIDE.md** - руководство по интеграции

### Примеры для других сервисов:
- **chat-service/MAXBOT_INTEGRATION_EXAMPLES.md** - примеры для Chat Service
- **employee-service/MAXBOT_INTEGRATION_EXAMPLES.md** - примеры для Employee Service

## 🎯 Следующие шаги

1. **Прочитайте** `QUICK_START_MAXBOT_INTEGRATION.md` для быстрого старта
2. **Запустите** MaxBot Service с вашим токеном Max API
3. **Протестируйте** базовые методы через grpcurl
4. **Интегрируйте** в другие сервисы по примерам из документации

## 🐳 Docker

Для запуска через Docker Compose:

```bash
# Установите токен в .env файл
echo "MAX_BOT_TOKEN=your-bot-token" > .env

# Запустите все сервисы
docker-compose up -d

# Проверьте логи
docker-compose logs -f maxbot-service
```

## 💡 Важные замечания

1. **MAX_BOT_TOKEN обязателен** - сервис не запустится без него
2. **Proto файлы уже сгенерированы** - не нужно их регенерировать, если не меняете proto
3. **Код готов к использованию** - все методы реализованы и протестированы
4. **Документация полная** - все примеры и use cases описаны

## 🆘 Troubleshooting

### Проблема: Proto файлы не генерируются

**Решение:**
```bash
# Добавьте Go bin в PATH
export PATH=$PATH:~/go/bin

# Или используйте полный путь
export PATH=$PATH:$(go env GOPATH)/bin
```

### Проблема: MaxBot Service не запускается

**Решение:**
```bash
# Проверьте токен
echo $MAX_BOT_TOKEN

# Проверьте, что порт свободен
lsof -i :9095

# Проверьте логи
go run cmd/maxbot/main.go
```

### Проблема: Ошибки при вызове методов

**Решение:**
- Убедитесь, что MaxBot Service запущен
- Проверьте правильность адреса (localhost:9095)
- Проверьте таймауты
- Проверьте логи обоих сервисов

## 📞 Поддержка

- **Документация Max API**: https://dev.max.ru/
- **GitHub Issues**: Создайте issue в репозитории
- **Вопросы**: Обратитесь к команде разработки

---

**Готово к использованию!** 🎉

