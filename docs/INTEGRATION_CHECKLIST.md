# MaxBot Service Integration Checklist

Чеклист для интеграции новых методов MaxBot Service в другие сервисы проекта.

## ✅ Выполнено

### MaxBot Service
- [x] Обновлен proto файл с новыми методами
- [x] Расширен domain интерфейс MaxAPIClient
- [x] Реализованы все новые методы в infrastructure/maxapi/client.go
- [x] Добавлены методы в usecase слой
- [x] Реализованы gRPC handlers
- [x] Обновлена документация README.md
- [x] Создано руководство по интеграции INTEGRATION_GUIDE.md

### Документация
- [x] Создан MAXBOT_INTEGRATION_SUMMARY.md с обзором изменений
- [x] Создан INTEGRATION_CHECKLIST.md (этот файл)
- [x] Создан chat-service/MAXBOT_INTEGRATION_EXAMPLES.md
- [x] Создан employee-service/MAXBOT_INTEGRATION_EXAMPLES.md
- [x] Обновлен главный README.md проекта

## 📋 Требуется выполнить

### Общие задачи

- [ ] **Сгенерировать proto файлы**
  ```bash
  ./generate_proto.sh
  ```
  Или установить protoc:
  ```bash
  brew install protobuf  # macOS
  ```

- [ ] **Протестировать MaxBot Service**
  ```bash
  cd maxbot-service
  export MAX_BOT_TOKEN="your-bot-token"
  go run cmd/maxbot/main.go
  ```

### Employee Service

- [ ] **Добавить новые методы в MaxClient**
  - Скопировать код из `employee-service/MAXBOT_INTEGRATION_EXAMPLES.md`
  - Добавить `SendNotification` и `CheckPhoneNumbers`

- [ ] **Обновить domain интерфейс**
  - Расширить `internal/domain/max_service.go`

- [ ] **Реализовать use cases**
  - `NotifyEmployee`
  - `NotifyUniversityEmployees`
  - `ValidateEmployeePhones`

- [ ] **Добавить HTTP endpoints**
  - POST `/employees/{id}/notify`
  - POST `/universities/{id}/notify`
  - GET `/universities/{id}/validate-phones`

- [ ] **Добавить метод в репозиторий**
  - `GetByUniversityID` в EmployeeRepository

- [ ] **Обновить Swagger документацию**
  ```bash
  cd employee-service
  make swagger
  ```

### Chat Service

- [ ] **Добавить новые методы в MaxClient**
  - Скопировать код из `chat-service/MAXBOT_INTEGRATION_EXAMPLES.md`
  - Добавить `SendNotification`, `GetChatInfo`, `GetChatAdmins`

- [ ] **Обновить domain интерфейс**
  - Расширить `internal/domain/max_service.go`
  - Добавить типы `ChatInfo` и `ChatMember`

- [ ] **Реализовать use cases**
  - `SyncChatFromMax`
  - `SyncChatAdmins`
  - `NotifyAdministrators`
  - `NotifyAdministratorSync`

- [ ] **Добавить HTTP endpoints**
  - POST `/chats/{id}/sync`
  - POST `/chats/{id}/notify`

- [ ] **(Опционально) Добавить фоновую синхронизацию**
  - Создать `internal/app/sync_worker.go`
  - Запустить worker в main.go

- [ ] **Обновить Swagger документацию**
  ```bash
  cd chat-service
  make swagger
  ```


### Auth Service

- [ ] **(Опционально) Добавить интеграцию с MaxBot**
  - Создать `internal/infrastructure/max/max_client.go`
  - Добавить domain интерфейс
  - Реализовать уведомления о входе

- [ ] **(Опционально) Добавить 2FA через Max Messenger**
  - Отправка кодов подтверждения
  - Валидация кодов

### Structure Service

- [ ] **Изучить потребности сервиса**
  - Определить, какие методы Max API нужны
  - Создать план интеграции

- [ ] **Реализовать интеграцию** (если необходимо)
  - Создать MaxClient
  - Добавить use cases
  - Добавить HTTP endpoints

## 🧪 Тестирование

### MaxBot Service

- [ ] **Unit тесты**
  ```bash
  cd maxbot-service
  go test ./...
  ```

- [ ] **Тестирование через grpcurl**
  ```bash
  # Установить grpcurl
  brew install grpcurl  # macOS
  
  # Тестировать методы
  grpcurl -plaintext -d '{"phone": "+79991234567"}' \
      localhost:9095 maxbot.MaxBotService/GetMaxIDByPhone
  ```

### Employee Service

- [ ] **Тестировать новые endpoints**
  ```bash
  # Уведомление сотруднику
  curl -X POST http://localhost:8081/employees/1/notify \
    -H "Content-Type: application/json" \
    -d '{"message": "Test"}'
  
  # Валидация номеров
  curl http://localhost:8081/universities/1/validate-phones
  ```

### Chat Service

- [ ] **Тестировать новые endpoints**
  ```bash
  # Синхронизация чата
  curl -X POST http://localhost:8082/chats/1/sync
  
  # Уведомление администраторов
  curl -X POST http://localhost:8082/chats/1/notify \
    -H "Content-Type: application/json" \
    -d '{"message": "Test"}'
  ```

## 🐳 Docker

- [ ] **Обновить docker-compose.yml**
  - Убедиться, что все сервисы имеют переменные MAXBOT_GRPC_ADDR
  - Проверить зависимости (depends_on)

- [ ] **Пересобрать образы**
  ```bash
  docker-compose build
  ```

- [ ] **Запустить все сервисы**
  ```bash
  docker-compose up -d
  ```

- [ ] **Проверить логи**
  ```bash
  docker-compose logs -f maxbot-service
  docker-compose logs -f employee-service
  docker-compose logs -f chat-service
  ```

## 📚 Документация

- [ ] **Обновить README сервисов**
  - Добавить информацию о новых endpoints
  - Обновить примеры использования

- [ ] **Обновить Swagger**
  - Сгенерировать новую документацию
  - Проверить доступность по `/swagger/`

- [ ] **Создать примеры использования**
  - Добавить примеры в документацию
  - Создать Postman коллекцию (опционально)

## 🔒 Безопасность

- [ ] **Проверить маскировку номеров в логах**
  - Убедиться, что номера телефонов маскируются
  - Проверить логи на наличие чувствительных данных

- [ ] **Проверить обработку ошибок**
  - Убедиться, что токены не попадают в ошибки
  - Проверить sanitization сообщений об ошибках

- [ ] **Настроить переменные окружения**
  - MAX_BOT_TOKEN должен быть в secrets
  - Не коммитить токены в git

## 📊 Мониторинг

- [ ] **Добавить метрики** (опционально)
  - Количество отправленных сообщений
  - Количество ошибок Max API
  - Время ответа Max API

- [ ] **Настроить алерты** (опционально)
  - Алерты при недоступности Max API
  - Алерты при превышении rate limits

## 🚀 Деплой

- [ ] **Протестировать на staging**
  - Проверить все новые функции
  - Убедиться в корректной работе

- [ ] **Обновить production**
  - Применить миграции (если есть)
  - Обновить сервисы
  - Проверить работоспособность

## 📝 Примечания

### Важные моменты

1. **Proto файлы**: После изменения proto файлов обязательно запустите `./generate_proto.sh`
2. **Таймауты**: Используйте разумные таймауты (5-10 секунд)
3. **Graceful degradation**: Сервисы должны работать даже при недоступности Max API
4. **Кэширование**: Рассмотрите кэширование Max ID для уменьшения нагрузки
5. **Rate limiting**: Max API имеет ограничения на количество запросов

### Полезные ссылки

- MaxBot Service README: `maxbot-service/README.md`
- Integration Guide: `maxbot-service/INTEGRATION_GUIDE.md`
- Chat Service Examples: `chat-service/MAXBOT_INTEGRATION_EXAMPLES.md`
- Employee Service Examples: `employee-service/MAXBOT_INTEGRATION_EXAMPLES.md`
- Max Messenger Bot API: https://dev.max.ru/

### Контакты

Для вопросов и проблем создайте issue в репозитории проекта.

