# Quick Start - Gateway Service Swagger

## 🚀 Быстрый запуск

### Вариант 1: Тестовый сервер (рекомендуется для демо)

```bash
cd gateway-service
go run test_gateway_swagger.go
```

Откройте: **http://localhost:8085/swagger/**

### Вариант 2: Полный Gateway Service

```bash
cd gateway-service
go run cmd/gateway/main.go
```

Откройте: **http://localhost:8080/swagger/**

*Примечание: Может показывать предупреждения о недоступных backend сервисах, но swagger будет работать.*

### Вариант 3: Статические файлы

```bash
cd gateway-service
make docs-serve
```

Откройте: **http://localhost:8082**

## 📖 Что доступно

- **40+ API эндпоинтов** всех сервисов
- **Интерактивное тестирование** API
- **Примеры запросов** и ответов
- **Схемы данных** с валидацией
- **Аутентификация** через Bearer токен

## 🔧 Основные эндпоинты

### Auth
- `POST /register` - Регистрация
- `POST /login` - Вход
- `POST /auth/max` - MAX аутентификация

### Employees
- `GET /employees/all` - Список сотрудников
- `POST /simple-employee` - Создать сотрудника

### Chats
- `GET /chats` - Список чатов
- `POST /chats` - Создать чат

### Universities
- `GET /universities` - Список университетов
- `POST /universities` - Создать университет

## ✅ Готово к использованию!

Swagger документация полностью интегрирована и готова для разработки и тестирования API.